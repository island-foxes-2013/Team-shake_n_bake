
<div id='question_box'>

  <h1><%= @question.title %></h1>
  <hr />
  <div id="grav_vote_box">
    <div id='count'>
      <span class="badge"><%= @question.votes.count %></span>
    </div>
    <div id='vote_btn'>
      <%= button_to "up", question_votes_path(@question, { vote: 'up'}), class: "btn btn-success btn-xs",  id: 'voteup', remote: true %>
      <%= button_to "dwn", question_votes_path(@question, { vote: 'dwn'}), class: "btn btn-danger btn-xs", id: 'votedwn', remote: true %>
    </div>
    <p><%= image_tag(@question.user.gravatar_url, width: "40", height: "40") %><span> - <%= @question.user.username %></span></p><div class='view_info_box'><span><%= @question.view %></span><div>Views</div></div><div class='view_info_box'><span><%= @question.answers.count %></span><div>Answers</div></div>
  </div>

  <% if @question.user.image.present? %>
  <img src='<%= @question.user.image.thumb.to_s %>'>
  <% else %>
  <p><%= image_tag(@question.user.gravatar_url, width: "40", height: "40") %>
    <% end %>
    <p id='question_body'><%= @question.body %></p><hr />

    <img src='<%= @question.image_url.to_s %>'>
 
    <% @question.comments.each do |comment| %>
    <p id='comment_append'><%= comment.content %>
      <br><span id='comment_date'>comment by: <b>jim</b> on <%= comment.created_at.strftime('%B, %d, %Y') %>, </span></p>
      <hr />
      <%end%>

 





    <% if session[:id] %>
    <%= form_tag question_comments_path(@question), id: 'comment_form', remote: true do %>
    <%= submit_tag 'Add Comment', class: "btn btn-primary btn-xs" %>
    <%= text_area_tag 'comment', nil, class: "form-control"%>
    <% end %>
    <% end %>
  </div>
   </div>
